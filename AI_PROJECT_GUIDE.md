# Atomosidea — AI向けプロジェクト入門ガイド

このドキュメントは、**Atomosideaプロジェクトについて何も知らないAI**が、これを読完するだけでプロジェクトを完全理解し、仕様設計やエラーへの注意を把握した上でコード変更を行えることを目的に書かれています。

読み終えた時点で、次のことができるはずです:
- プロジェクトの全体像と各サービスの役割を説明できる。
- ユーザーがファイルをアップロードしてから公開されるまでの流れを記述できる。
- 各サービスのパス・DB・MinIO・Redisキュー・APIを正しく引用できる。
- このプロジェクト特有の「やりやすい失敗（トラップ）」を回避できる。
- 安全にコード変更・機能追加を行える。

> **このドキュメントは「教え方」に特化しています。** 現行の実装詳細（正確なパス、テーブル列、環境変数名）は `EditAI.md` を参照してください。本ドキュメントは「どう理解し、どう考えるか」を優先します。

---

## 0. 読み方と前提

- このドキュメントは**順序立てて読む**ことを推奨します。0→1→2…の順で進めてください。
- 技術用語（マイクロサービス、MQ、S3等）が出てきたら、その場で理解しようとせず、文脈から推測してください。後述の「用語集」も活用してください。
- 「⚠️」はAIが実際に失敗しやすい箇所、「🔑」は設計の核心（なぜそうなっているか）です。

---

## 1. プロジェクトとは（一言で言うと）

**Atomosideaは、「動画・ゲーム・静态サイト」を人がアップロードして公開・閲覧できるウェブプラットフォーム**です。

- ユーザーは自分の作ったUnity WebGLゲームや動画、静态サイトをアップロードする。
- システムがそれをスキャン・変換・展開し、「公開」になってから他ユーザーが見られる。
- 作者自身も、自分の投稿の一覧をマイページで見られ、編集・削除できる。

これは **Vimeo/YouTube（動画）+ Itch.io（ゲーム）+ GitHub Pages（静态サイト）を1つのアプリにまとめた**ようなものとイメージすると分かりやすいです。

---

## 2. なぜマイクロサービスなのか（設計思想）

単一の巨大なプログラム（モノリシック）にせず、**各機能を独立した小さなサービス（コンテナ）に分けています**。

🔑 **理由:**
- **独立してスケールできる**: 動画変換は重いがゲーム処理は軽い、といった状況でも、重いサービスだけ増やせる。
- **壊れ方を隔離できる**: 1つのサービスが落ちても、他のサービスは動き続ける。
- **並行開発できる**: チーム（またはAI）が各サービスを同時に開発できる。
- **セキュリティを隔離できる**: マルウェアを処理するSFSPだけを特別に隔離できる（後述）。

⚠️ **代償（AIが理解すべきトレードオフ）:**
- サービス間通信（HTTP・キュー）が必要で、コードが分断される。
- デプロイ・設定が複雑になる。
- **サービス間契約（Interface Contract）を守らないと壊れる**: キュー名・レスポンス形式・DBスキーマは「契約」なので、一方だけ変えると全体が壊れます。

---

## 3. 全体像（頭の中図を作る）

```
                        [ユーザーのブラウザ]
                                 │
                                 ▼
        ┌───────────────────────────────────────────┐
        │  frontend (Nginx)  ← 入口。ポート3001      │
        │  ・Reactアプリを配信                        │
        │  ・/api/... を各サービスに振り分ける        │
        │  ・HLS動画・静态サイトを配信                │
        └───────────────────────────────────────────┘
         │      │      │       │        │
         ▼      ▼      ▼       ▼        ▼
      auth   profile  videos  games   static-sites   ← APIサービス（REST）
      │       │                          │
      ▼       ▼                          ▼
   [DB: PostgreSQL × 4]   [Redis: キュー]   [MinIO: ストレージ × 6]
                                 │
                                 ▼
        ┌─────────────────────────────────────┐
        │  SFSP（セキュリティスキャン）        │
        │  ・ファイルをスキャン（ClamAV/YARA） │
        │  ・Docker Sandbox内で隔離実行        │
        └─────────────────────────────────────┘
                                 │
                                 ▼
        [worker: 変換・展開] → 公開済みファイルをMinIOへ
```

🔑 **重要概念: 「API」と「worker」の分離**
- **upload-api** は「受け付ける」だけ。重い処理（スキャン・変換・展開）はしません。
- **worker** は「裏で動く処理役」。RedisというキューThroughでapiと切り離されています。
- これにより、ユーザーはアップロード instantly に反応し、重い処理はバックグラウンドで行えます。

---

## 4. 用語集（先に読んでおく）

| 用語 | 意味 |
|---|---|
| **マイクロサービス** | 小さなプログラムを複数組み合わせて1つのシステムを構成する方式。 |
| **コンテナ (Docker)** | アプリを依存関係ごと隔離して実行する箱。再現性が高い。 |
| **Docker Compose** | 複数のコンテナを1つの設定ファイル(`docker-compose.yml`)で一括管理。 |
| **MQ (メッセージキュー)** | サービス間で非同期にメッセージをやり取りする仕組み。Redisで実装。 |
| **S3 / MinIO** | Amazon S3互換のオブジェクトストレージ。画像・動画・ゲームファイルを保存。 |
| **HLS** | 動画を小さな区切り(.ts)に分け、品質に応じて配信する方式。`.m3u8`は再生リスト。 |
| **JWT** | 認証トークン。署名付きの小さなデータ。提示することで「本人確認」。 |
| **Sandbox (サンドボックス)** | 不正プログラムを隔離して実行する安全な箱。 |
| **ClamAV / YARA** | アンチウイルス(ClamAV)とマルウェアパターン検出(YARA)のツール。 |
| **Nginx** | ウェブサーバー。リクエストを振り分ける「受付係」的役割も担う。 |
| **Redis** | 超高速なメモリ上のデータストア。キューとキャッシュに使用。 |
| **PostgreSQL** | 高機能なリレーショナルデータベース。 |

---

## 5. サービス一覧（誰が何を担当するか）

全19サービス。**役割別にグループ化して覚えると分かりやすい**です。

### 5.1. ユーザーFacing（直接アクセスされるもの）
| サービス | 役割 | 入口パス |
|---|---|---|
| `frontend` | Nginx＋React。全体の入口。 | ホスト3001 |
| `auth-service` | ログイン/登録/JWT/アカウント削除 | `/api/auth/*` |
| `profile-service` | プロフィール管理＋アイコンスキャン | `/api/profile/*` |
| `mypage-service` | マイページ（自分の投稿一覧） | `/api/my/*` |

### 5.2. コンテンツアップロード（受け付け＋裏処理）
| サービス | 役割 | 入口パス |
|---|---|---|
| `video-upload-api` + `video-worker` | 動画の受け付け＋HLS変換 | `/api/videos/*` |
| `game-upload-api` + `game-worker` | ゲームの受け付け＋展開 | `/api/games/*` |
| `static-site-upload-api` + `static-site-worker` | 静态サイトの受け付け＋展開 | `/api/static-sites/*` |

> **ペア構造に注意:** 各コンテンツは「upload-api（受け付け）」と「worker（処理）」の2つで1セット。workerはRedis経由でapiから仕事をもらいます。

### 5.3. セキュリティ（隔離された中核）
| サービス | 役割 |
|---|---|
| `sfsp-api` | ファイル受け付け・重複排除・ジョブキューイング |
| `sfsp-worker` | ClamAV/YARAでスキャン（Docker Sandbox内） |
| `sfsp-clamav-client` / `sfsp-yara-client` | スキャナイメージ（ビル 전용） |

### 5.4. インフラ
| 種別 | 内容 |
|---|---|
| **DB (PostgreSQL ×4)** | `auth-db`(ユーザー), `app-db`(コンテンツメタ), `profile-db`(プロフィール), `sfsp-db`(スキャン結果) |
| **MinIO ×6** | `profile-storage`, `game-storage`, `static-site-storage`, `video-storage`, `sfsp-raw-minio`, `sfsp-clean-minio` |
| **Redis** | キュー＋JWTブロックリスト |
| **monitoring-service** | 運用ダッシュボード（任意） |

⚠️ **数字を覚える理由:** 「4基のDB」「6基のMinIO」は、このプロジェクトのスケールを示す指標です。AIが「DBを増やす」「新規ストレージ追加」を提案・実装する際は、この数字と各サービスの依存関係を正しく理解しているか問われます。

---

## 6. 核心ワークフロー（これで全体の8割が分かる）

### 6.1. ゲームアップロード（最も重要・非同期）

🔑 **このフローが理解できれば、動画・静态サイトも同じパターンです。**

```
ユーザー → frontend → game-upload-api
                              │
                              ├─▶ sfsp-api  「スキャンして」
                              │        └─▶ Redis: sfsp:scan_queue
                              └─▶ app-db.games 「status=scanning」で記録
                                                    │
                                              sfsp-worker
                                                    │  Docker SandboxでClamAV/YARA
                                                    ▼
                                         cleanなら sfsp-clean-minio/clean-files
                                                    │
                                        Redis: sfsp:completed:game
                                                    │
                                              game-worker
                                                    │  展開・解像度抽出・CSS注入
                                                    ▼
                                         game-storage/games に配置
                                         app-db.games → status=public
                                                    │
                                              frontendがポーリング
                                              (statusがpublicになるまで)
                                                    │
                                                  表示・プレイ可能
```

🔑 **設計の核心:**
1. **受け付けと処理は分離**: apiは即座に`gameId`を返す。重い処理はworkerが裏で行う。
2. **スキャンは必須**: すべてのアップロードはSFSPによりスキャンされる。`clean`以外は公開されない。
3. **ステータStateMachine**: `scanning → processing → public`（または `quarantined`）。フロントはこの状態をポーリングして表示を切り替える。
4. **Redisが接続役**: apiとworkerはRedisキューで繋がる。これで両者は直接呼び合わず、分離されたまま協調できる。

### 6.2. 認証フロー（簡易）
```
ユーザー → /api/auth/login → auth-service → auth-db 検証
                                    │ 成功
                                    ▼
                             JWT発行 → frontendがlocalStorageに保存
                                    │ 以降のAPIで Authorization: Bearer <token> を付与
                                    ▼
                         各サービスがJWT署名を検証 → アクセス許可/拒否
```

### 6.3. SFSPスキャン（セキュリティの中核）
```
upload-api ──POST /api/v1/files──▶ sfsp-api
        │  SHA256を計算 → sfsp-dbで重複チェック
        └─▶ 新規なら sfsp-raw-minio/raw-files に保存
                └─▶ Redis: sfsp:scan_queue にジョブID
                                │
                        sfsp-worker が取得
                                │  Docker Sandbox内で ClamAV + YARA
                                ▼
                     clean → sfsp-clean-minio/clean-files
                     malicious → sfsp-clean-minio/quarantine
                                │
                    Redis: sfsp:completed:{game,stream,...} にイベント
```

🔑 **なぜこんなにも分離しているか:** マルウェアを処理するため、スキャン専用サービスを別ネットワーク(`sfsp-isolated-net`)に隔離。ただし処理結果は公開先へ渡す必要があるため、一部は一般ネットワークにも参加します。

---

## 7. このプロジェクト特有の「やりやすい失敗（トラップ）」

AIが変更を行う際、特に注意すべき**プロジェクト固有の制約**です。これらを理解せずに変更すると、論外のバグを入れます。

### ⚠️ Trap 1: Nginxの `proxy_pass` 末尾スラッシュ
- **規約**: `proxy_pass http://xxx;` の**末尾にスラッシュを付けない**。
- **理由**: 付けると、`location`でマッチした部分(`/api/auth`)が削られ、バックエンドに(`/me`)しか届かず、Goのルーティングが壊れる。
- **AIの対応**: Nginxを編集するたびに、すべての `proxy_pass` が末尾スラッシュなしか確認。**編集後は `docker-compose restart frontend` を必須で実行。**

### ⚠️ Trap 2: Goの `shared` 依存とDockerビルド
- 全Goサービスは `github.com/atmosidea/shared` を共有する。
- `go.mod` 末尾に `replace github.com/atmosidea/shared => <相対パス>` が必要。**忘れると `exit status 128`（GitHub認証エラー）でビルド失敗。**
- Dockerビルドは **常にルートコンテキスト** で `COPY . .` 後 `WORKDIR /app/<パス>`。`RUN go mod tidy` は**無い**（コンテナ内ではGitHubへ通信するため）。
- **AIの対応**: 新Goサービス作成時、`replace` を正しく追記。既存Dockerfileの `COPY . .` / `WORKDIR` / ビルドコンテキストを破換しない。

### ⚠️ Trap 3: 「正のソース」は実装
- ドキュメント（本編・README・旧EditAI.md）と実装(`docker-compose.yml`・`backend/`)が**食い違った場合、実装が正**。
- 特に旧EditAI.mdは古い構成（`auth-service` が別ディレクトリ等）を記載していることがある。
- **AIの対応**: 不確かなパス・名前は、必ず `docker-compose.yml` と `backend/` の実ディレクトリで確認する。

### ⚠️ Trap 4: サービス間契約
- キュー名(`sfsp:scan_queue`等)、レスポンスJSON、DBスキーマは「契約」。
- **AIの対応**: 一方だけ変更せず、関連全サービスで整合を取る。変更後は関連サービスをすべて再ビルド。

### ⚠️ Trap 5: 静态サイトのサブドメイン分離
- 静态サイトは `http://{siteId}.localhost:3001/` サブドメインで配信。
- `server_name` の正規表現は**引用符で囲む**（バックスラッシュ含むため）。
- **AIの対応**: Nginx編集後、構文エラーが出ていないか確認。

### ⚠️ Trap 6: 動画はMinIOではなくローカルファイル
- 動画HLSはMinIOではなく**ローカルファイルシステム**に配置。Nginxは `alias` でマッピング（`proxy_pass` ではない）。
- **AIの対応**: 動画配信関連を編集する際、`location ~ ^/api/videos/...` の `alias` ブロックを必ず確認。

### ⚠️ Trap 7: `if` 文（Nginxの技術的負債）
- `/api/videos/:id` で `GET`(video-worker) と `DELETE`(video-upload-api) が別サービス。暫定的に `if ($request_method = DELETE)` で分岐。
- **「if is evil」**（予期せぬ挙動の原因）。動画ルーティングを変更する際は影響確認。

---

## 8. セキュリティ（理解すべき3点）

### 8.1. JWT + localStorage のトレードオフ
- **メリット**: CSRF攻撃を原理的に阻止（Cookie自動送信に依存しない）。
- **デメリット**: XSSが1箇所でも成功すると `localStorage` のJWTが一瞬で盗まれる。
- **対策**: Reactの自動エスケープ、厳格なCSP、`npm audit`。

### 8.2. iframe sandbox
- アップロード静态サイトを `sandbox` 付きiframeで表示。
- ただしカメラ機能等のため `allow-same-origin` を許可（→ サブドメイン分離で補強）。

### 8.3. SFSPサンドボックス
- `ReadonlyRootfs: true`、ファイル`:ro`マウント、リソース制限（DoS対策）、`tmpfs`。

🔑 **AIの対応**: 認証・認可・sandbox関連を変更する際は、上記トレードオフを踏まえた上で。理由 Without understanding, you will introduce vulnerabilities.

---

## 9. 変更を行う前チェックリスト（AI用）

1. **正のソース確認**: パス・DB名・キュー名・環境変数は `docker-compose.yml` と `backend/` で確認。
2. **影響範囲特定**: 変更が「どのサービスの契約」に関わるか特定。関連全サービスをリストアップ。
3. **Goサービス**: `go.mod` の `replace` を確認。`Dockerfile` のビルド方式を破換しない。
4. **Nginx編集**: 末尾スラッシュなし `proxy_pass` 規約遵守。編集後 `docker-compose restart frontend`。
5. **DBスキーマ**: 対応DBの `init.sql` を更新（auth-db/app-db/profile-db/sfsp-db）。
6. **MinIO政策**: バケット/ユーザー/ポリシー変更は `docker-compose.yml` の `minio-init` と `backend/minio-init/*.json` を対で更新。
7. **契約の整合性**: キュー名・レスポンス形式・スキーマを関連全サービスで一致させる。
8. **大規模消去**: 必ずユーザーの許可を取得。

---

## 10. 実践演習（理解度チェック）

次の質問に答えられれば、プロジェクトを理解しています:

1. **Q**: ユーザーがゲームをアップロードして「プレイ可能」になるまで、どのサービスを通り、各サービスは何をするか記述せよ。
   - *Aの要点: upload-api(sfsp依頼+scanning記録) → sfsp-api(SHA256+キュー) → sfsp-worker(Sandboxスキャン) → completed:game → game-worker(展開+解像度抽出+public) → frontendポーリング → 表示*

2. **Q**: なぜ「upload-api」と「worker」を分離しているか、1文で説明せよ。
   - *A: ユーザー即座の反応を得ため受け付けと重い裏処理を分離し、Redisキューで非同期協調するため。*

3. **Q**: Nginxの `proxy_pass` 末尾にスラッシュを付けない理由を説明せよ。
   - *A: 付けるとlocationでマッチした部分が削られ、バックエンドが/apiプレフィックスを前提とするルーティングが壊れるため。*

4. **Q**: SFSPを別ネットワークに隔離する理由を説明せよ。
   - *A: マルウェアを処理するため他サービスから隔離し、侵害範囲を限定するため。*

5. **Q**: 新Goサービスを追加するとき、ビルドが `exit status 128` で失敗する原因と解決を説明せよ。
   - *A: sharedへのreplace指定不足でGitHubへ通信しようするため。`go.mod` に正しいreplaceを追記して解決。*

---

## 11. 用語と概念の深掘り（応用）

### 11.1. なぜ4基のDBなのか
- **分離の原則**: ユーザーデータ(auth-db)、コンテンツメタ(app-db)、プロフィール(profile-db)、スキャン結果(sfsp-db)を分離。
- **メリット**: 片方の負荷が他へ影響しない、バックアップ/監査が簡単、権限分離が容易。

### 11.2. なぜ6基のMinIOなのか
- **隔離と最小権限**: 各ストレージで別アクセスキー。侵害範囲を限定。
- **物理分離**: 各ストレージが別ディレクトリに保存。バックアップ/管理が簡単。

### 11.3. なぜDocker Sandboxでスキャンするか
- **隔離実行**: マルウェアがホストや他サービスに影響しない。
- **ReadonlyRootfs**: コンテナ自体の改ざん防止。
- **リソース制限**: ZIP爆弾等のDoS攻撃から保護。

### 11.4. なぜサブドメイン分離なのか
- **同一オリジンポリシー**: メインアプリと静态サイトをオリジン分離。
- **sandbox強化**: `allow-same-origin` のリスクを低減。

---

## 12. まとめ（記憶すべき核心）

1. **Atomosidea = 動画・ゲーム・静态サイトを人がアップロードして公開するプラットフォーム。**
2. **マイクロサービス**: 各機能は独立したコンテナ。独立してスケール・隔離できる。
3. **API/worker分離**: 受け付けと重い処理はRedisキューで分離。
4. **SFSP**: すべてのアップロードをスキャン。Docker Sandboxで隔離実行。
5. **4DB・6MinIO**: 関心事ごとに分離。最小権限。
6. **Nginxが入口**: APIゲートウェイ＋静态配信。末尾スラッシュなし `proxy_pass`。
7. **JWT+localStorage**: CSRF回避とXSSリスクのトレードオフ。
8. **正のソースは実装**: ドキュメントと実装が食い違った場合、実装が正。

---

> **次の一歩**: 本ガイドで概念を理解したら、`EditAI.md` で現行の実装詳細（正確なパス・列・環境変数）を確認し、実際のコード `backend/` と対照しながら読んでください。変更を行う前に必ず「第8章 変更前チェックリスト」を確認してください。