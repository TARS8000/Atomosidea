# Atmosidea セキュリティ設計説明書

## 1. 概要

このドキュメントは、Atmosideaプロジェクト全体のセキュリティアーキテクチャと、その設計思想について説明します。本プロジェクトは、ユーザーがコンテンツを安全にアップロード・共有できるプラットフォームを提供するため、多層的なセキュリティ対策を講じています。

主要なセキュリティ原則は以下の通りです。

*   **多層防御 (Defense in Depth):** 単一のセキュリティ対策に依存せず、認証、認可、入力検証、インフラ保護など、複数の防御層を設けます。
*   **最小権限 (Least Privilege):** 各コンポーネント（サービス、ユーザー）は、その機能に必要な最小限の権限のみを持つべきです。
*   **セキュア・バイ・デフォルト (Secure by Default):** 設定を何もしなければ、最も安全な状態がデフォルトとなるように設計します。
*   **信頼しない (Zero Trust):** サービス間通信であっても、常にリクエストを検証し、明示的に信頼されたリクエストのみを受け入れます。

---

## 2. 主要セキュリティワークフロー

システムの安全性を確保するための主要な処理の流れを以下に詳述します。

### 2.1. ワークフロー1: ユーザー認証とAPIアクセス

このワークフローは、ユーザーがどのように認証され、保護されたAPIリソースにアクセスするのかを示します。

1.  **[ユーザー → `frontend`]**
    *   ユーザーがブラウザでログインページを開き、メールアドレスとパスワードを入力して「ログイン」ボタンをクリックします。

2.  **[`frontend` → `auth-service`]**
    *   `frontend`は、入力された認証情報をペイロードに含め、`auth-service`の `/api/auth/login` エンドポイントにPOSTリクエストを送信します。

3.  **[`auth-service`]**
    *   リクエストを受け取ると、`auth-db`（PostgreSQL）に保存されているユーザー情報を検索します。
    *   提供されたパスワードと、データベースに保存されているパスワードハッシュを `bcrypt` を用いて比較・検証します。

4.  **[`auth-service` → `frontend`]**
    *   認証に成功した場合、`auth-service`はユーザーIDや有効期限（例: 24時間）を含むペイロードを持つJWT（JSON Web Token）を生成します。
    *   このJWTに、`.env` ファイルで管理されている `JWT_SECRET` を使って署名し、レスポンスとして `frontend` に返します。

5.  **[`frontend`]**
    *   受け取ったJWTを安全な場所（例: メモリ、HttpOnlyクッキー）に保存します。

6.  **[`frontend` → `mypage-service` (保護されたAPI)]**
    *   ユーザーがマイページにアクセスすると、`frontend`は `mypage-service` の `/api/my/videos` エンドポイントにGETリクエストを送信します。
    *   この際、HTTPリクエストヘッダーに `Authorization: Bearer <JWT>` を付与します。

7.  **[`mypage-service`]**
    *   リクエストを受け取ると、まずAPIミドルウェアが `Authorization` ヘッダーのJWTを検証します。
    *   `mypage-service`も同じ `JWT_SECRET` を保持しており、これを使ってトークンの署名を検証します。
    *   署名が有効で、かつ有効期限内であれば、ペイロードからユーザーIDを抽出し、そのユーザーに関連するデータ（この場合は動画一覧）を `app-db` から取得して返します。
    *   署名が無効、またはトークンが期限切れの場合、`401 Unauthorized` エラーを即座に返します。

### 2.2. ワークフロー2: 安全なファイルアップロードとスキャン

これは本システムで最も重要なセキュリティワークフローです。ユーザーがアップロードしたファイルが、いかにして安全に処理され、公開されるかを示します。

1.  **[ユーザー → `frontend` → `game-upload-api`]**
    *   認証済みユーザーが、ゲームのZIPファイル（例: `my_game.zip`）をアップロードします。
    *   `frontend`は、JWTと共にこのファイルを `game-upload-api` の `/api/games/upload` にPOSTします。

2.  **[`game-upload-api`]**
    *   JWTを検証し、リクエストが正当なユーザーからのものであることを確認します。
    *   `app-db`の`games`テーブルに、`status='processing'` としてコンテンツの仮レコードを作成します。
    *   受け取ったファイルを、スキャン依頼のメタデータ（`target_service='game'`）と共に、`sfsp-api` の `/api/v1/files` エンドポイントにHTTP転送します。

3.  **[`sfsp-api`]**
    *   ファイルを受け取り、その内容からSHA256ハッシュを計算します。
    *   `sfsp-db`の`files`テーブルで、このハッシュを持つファイルが既に存在するかをチェックします（重複排除）。
    *   **[新規ファイルの場合]**
        *   ファイルを `sfsp-minio` の `raw-files` バケット（未スキャン用）に保存します。
        *   `sfsp-db`の`files`テーブルにファイルメタデータ（ハッシュ、ファイル名等）を、`scan_jobs`テーブルに新しいジョブを `status='queued'` で記録します。
        *   Redisの `sfsp:scan_queue` リストに、このジョブのIDをプッシュします。
        *   `game-upload-api` に `202 Accepted` を返します。

4.  **[`sfsp-worker`]**
    *   Redisの `sfsp:scan_queue` を常時監視しており、新しいジョブIDをブロッキングPOPで取得します。
    *   `scan_jobs`テーブルのステータスを `running` に更新します。
    *   `sfsp-minio`の`raw-files`バケットから対象ファイルをダウンロードします。
    *   **サンドボックス内でスキャンを実行します。**
        *   a. **ClamAVスキャン:** `sfsp-clamav-client` Dockerイメージを、ファイルが読み取り専用でマウントされたサンドボックス内で起動し、ウイルススキャンを実行します。
        *   b. **YARAスキャン:** `sfsp-yara-client` Dockerイメージを同様のサンドボックス内で起動し、定義済みルールセット（`yara-rules/`）に基づいて不正なパターンを検出します。
    *   スキャン結果を `sfsp-db` の `scan_results` テーブルに記録します。

5.  **[`sfsp-worker` (後処理)]**
    *   すべてのスキャン結果を評価し、最終的なステータス（`clean`, `malicious`, `suspicious`など）を決定します。
    *   **[ステータスが `clean` の場合]**
        *   ファイルを `raw-files` バケットから `clean-files` バケット（スキャン済み用）にコピーします。
    *   **[ステータスが `malicious` の場合]**
        *   ファイルを `quarantine` バケット（隔離用）に移動します。
    *   `scan_jobs`テーブルのステータスを最終結果で更新します。
    *   スキャン完了イベント（`ScanCompletionEvent`）をJSON形式で作成し、Redisの `sfsp:completed_jobs` キューにプッシュします。このイベントには、ジョブID、最終ステータス、処理対象サービス（`target_service='game'`）が含まれます。

6.  **[`game-worker`]**
    *   Redisの `sfsp:completed_jobs` キューを常時監視しており、完了イベントを取得します。
    *   イベントの `final_status` が `clean` であることを確認します。
    *   `sfsp-minio` の `clean-files` バケットから安全なゲームZIPファイルをダウンロードします。
    *   ゲーム固有の処理（ZIP展開、`index.html`の修正など）を実行します。
    *   処理済みのゲームファイルを、公開用の `game-storage` MinIOバケットにアップロードします。
    *   `app-db`の`games`テーブルのレコードを、ステータスを `public` に、公開URLを最終的なパスに更新します。

7.  **[ユーザー → `frontend`]**
    *   ユーザーがマイページをリフレッシュすると、ゲームのステータスが「公開」に変わり、プレイ可能になります。

### 2.3. ワークフロー3: アカウントの完全削除

ユーザーデータのプライバシーを保護するため、アカウント削除は関連する全データをシステムから削除する重要なワークフローです。

1.  **[ユーザー → `frontend` → `auth-service`]**
    *   認証済みユーザーが「アカウント削除」を実行すると、`frontend`は `auth-service` の `/api/auth/me` エンドポイントにDELETEリクエストを送信します（JWT認証付き）。

2.  **[`auth-service`]**
    *   JWTを検証し、ユーザーIDを特定します。
    *   **削除処理を開始します。**
        *   a. `auth-db`の`users`テーブルで、対象ユーザーのステータスを `deleted_data` などに変更し、論理削除します。
        *   b. 関連する各サービス（`profile-service`, `upload-service`, `game-upload-api`など）に対して、内部的な削除リクエストを送信するか、またはRedisを通じて削除イベントを発行します。
        *   c. 各サービスは、受け取ったユーザーIDに基づいて、自身のデータベース（`app-db`）やストレージ（MinIOの各バケット）から、そのユーザーに紐づくすべてのコンテンツ（プロフィール画像、動画、ゲームなど）を物理的に削除します。
    *   すべての削除処理が完了したことを確認した後、`auth-service`は `auth-db` のユーザーレコードを物理的に削除します。

---

## 3. 個別コンポーネントのセキュリティ詳細

### 3.1. SFSPサンドボックス実行環境 (`sfsp-worker` -> `DockerSandbox`)

`sfsp-worker`は、スキャナを隔離されたDockerコンテナ内で実行することで、万が一マルウェアが実行されてもホストシステムや他のサービスに影響が及ばないようにしています。

*   **`ReadonlyRootfs: true`**: スキャナコンテナのファイルシステムは読み取り専用でマウントされ、コンテナ自体の改ざんを防ぎます。これはサンドボックスの最も重要な設定です。
*   **読み取り専用マウント (`:ro`)**: スキャン対象のファイルは、コンテナに読み取り専用でマウントされ、スキャナが元のファイルを変更することを防ぎます。
*   **リソース制限**: コンテナにはメモリ（2GB）やCPU（1CPU）の制限が課され、リソースを過剰に消費する攻撃（ZIP爆弾などのDoS攻撃）からシステム全体を保護します。
*   **`tmpfs` マウント**: `ReadonlyRootfs`の制約下でも、ClamAVが一時ファイルを書き込めるように、`/run/clamav` などのパスはRAM上の `tmpfs` としてマウントされます。これにより、コンテナは正常に動作しつつ、ホストへの永続的な書き込みは行われません。

### 3.2. アプリケーションの脆弱性対策

*   **SQLインジェクション対策:** データベース操作には、Goの `database/sql` パッケージが提供するプレースホルダ（`?` や `$1`）を常に使用します。SQLクエリを文字列結合で組み立てることはありません。
*   **クロスサイトスクリプティング (XSS) 対策:** フロントエンドではReactを採用しており、デフォルトでJSX内の文字列がエスケープされるため、多くのXSS攻撃は自動的に防止されます。`dangerouslySetInnerHTML`のような危険なプロパティの使用は意図的に避けています。
*   **入力検証:** ユーザーからの入力値は、APIの各ハンドラで型、長さ、フォーマットを検証してから処理します。

### 3.3. インフラと運用セキュリティ

*   **環境変数の管理:** データベースのパスワード、JWTの秘密鍵、外部APIキーなどの機密情報は、すべて`.env`ファイルで管理されます。このファイルは`.gitignore`に含まれており、Gitリポジトリにコミットされることはありません。
*   **Dockerセキュリティ:**
    *   **最小イメージの利用:** 各サービスのDockerイメージは、`alpine`などの軽量なベースイメージを基に構築され、攻撃対象領域を削減しています。
    *   **ネットワーク分離:** `docker-compose.yml`で定義された`atmosidea-network`により、コンテナ間の通信は許可されていますが、不要なポートは外部に公開されていません（例: データベースのポートは公開されていない）。

---

## 4. 将来の開発者への注意点

このプロジェクトのセキュリティは、各コンポーネントの密な連携の上に成り立っています。変更を加える際は、以下の点を特に意識してください。

*   **サンドボックス設定の変更は慎重に:** `internal/sandbox/docker.go`内の`ReadonlyRootfs`やリソース制限は、SFSPの安全性の根幹です。安易な変更は深刻な脆弱性を生む可能性があります。
*   **サービス間契約の遵守:** Redisのキュー名やイベントのJSON構造、APIのレスポンス形式などは、サービス間の「契約」です。変更する場合は、関連するすべてのサービスで整合性を取る必要があります。
*   **機密情報の取り扱い:** 新しい秘密鍵やAPIキーを追加する場合は、必ず`.env`ファイルで管理し、コード内にハードコーディングしないでください。

このドキュメントが、Atmosideaのセキュリティを維持し、さらに強化していく上での一助となることを願っています。